// ===== Entry point: runs once the HTML has fully loaded =====
document.addEventListener('DOMContentLoaded', function () {
  // Page currently has no interactive behavior.
  // This file is kept as a hook for future functionality
  // (e.g. filtering or searching the bibliography columns).
});